// export default function em(pixels) {
//   const baseFontSize = 25
//   const valueRelativeToRoot = pixels / rootFontSize
//   const baseFontSizeRelativeToRoot = baseFontSize / rootFontSize
//   const valueInEms = valueRelativeToRoot / baseFontSizeRelativeToRoot

//   // round to 4 decimal places
//   return Math.round(valueInEms * 10000) / 10000
// }